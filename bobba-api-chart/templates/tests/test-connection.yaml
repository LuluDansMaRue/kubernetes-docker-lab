apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bobba-api-chart.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "bobba-api-chart.name" . }}
    helm.sh/chart: {{ include "bobba-api-chart.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bobba-api-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
